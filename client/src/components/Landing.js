import React from 'react';

const Landing = () => {
	return (
		<div style={styles.title}>
			<h2>Email Express</h2>
			Collect feedback from your users
		</div>
	);
};

const styles = {
	title: {
		textAlign: 'center',
		color: '#9F7BC1'
	}
};

export default Landing;
